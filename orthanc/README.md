# Orthanc 설정

이 데모는 공식 `orthancteam/orthanc` Docker image를 사용하며, `docker-compose.yml`의 환경 변수로 공식 DICOMweb plugin을 활성화합니다.

로컬 접속 계정:

- Username: `orthanc`
- Password: `orthanc`

DICOMweb root:

- `http://localhost:8042/dicom-web`
